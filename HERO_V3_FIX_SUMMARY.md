# 히어로 캐러셀 v3 추가 수정 요약

## 수정 날짜
2025-11-12

## 해결된 문제 (4개)

### 1. ✅ Glow 스캔 방향 작동 불량
- **문제**: "왼쪽→오른쪽", "위→아래" 방향이 정상 작동하지 않음
- **원인**: 기본 `background-size: 200% 100%`가 모든 요소에 적용되어 방향별 오버라이드가 제대로 작동하지 않음
- **해결**: 각 방향에 background-size를 명시적으로 설정

### 2. ✅ 화살표 버튼 여백 문제
- **문제**: 화면 끝과 화살표 사이에 불필요한 여백 존재
- **원인**: v3에서 화살표를 화면 끝 기준으로 변경하면서 기본값이 5%로 설정됨
- **해결**: 전역 패딩 기준으로 재변경 + 기본값 0%로 수정

### 3. ✅ 모바일 게이지바 표시 안 됨
- **문제**: 모바일에서 게이지바 표시 옵션을 켜도 화면에 나타나지 않음
- **원인**: 조건 체크 및 위치 설정 로직 순서 문제
- **해결**: shouldShow 변수로 조건 명확화, 위치 설정은 표시할 때만 실행

### 4. ✅ 전역 패딩 일관성 문제
- **문제**: 슬라이드 캡션만 전역 패딩 영향을 받고, 화살표와 게이지는 영향 안 받음
- **원인**: v3에서 화살표를 의도적으로 화면 끝 기준으로 변경했지만, 사용자는 일관성을 원함
- **해결**: 화살표 위치 계산을 전역 패딩 + offset 방식으로 재변경

---

## 상세 변경 내역

### 📄 hero-carousel-v3-fixed.html

#### 1. CSS 수정 (line 265-291)

**변경 전:**
```css
.hero-cta .hero-cta-scan-highlight {
	background-size: 200% 100%;  /* 모든 요소에 적용 */
}

.hero-cta[data-glow-direction="left-to-right"] .hero-cta-scan-highlight {
	background-image: linear-gradient(90deg, ...);
	/* background-size 오버라이드 없음 */
}
```

**변경 후:**
```css
.hero-cta .hero-cta-scan-highlight {
	/* background-size는 각 방향별로 명시적 설정 */
}

.hero-cta[data-glow-direction="left-to-right"] .hero-cta-scan-highlight {
	background-image: linear-gradient(90deg, ...);
	background-size: 200% 100%;  /* 명시적 설정 */
}

.hero-cta[data-glow-direction="top-to-bottom"] .hero-cta-scan-highlight {
	background-image: linear-gradient(180deg, ...);
	background-size: 100% 200%;  /* 명시적 설정 */
}
```

#### 2. JavaScript 수정 - applyArrowPositions (line 857-877)

**변경 전:**
```javascript
function applyArrowPositions(confObj){
	const arrowOffsetX = confObj.navArrowOffsetXPercent || 5;

	// 화면 끝에서 직접 계산
	leftArrow.style.left = arrowOffsetX + '%';
	rightArrow.style.right = arrowOffsetX + '%';
}
```

**변경 후:**
```javascript
function applyArrowPositions(confObj){
	const globalPadX = parseFloat(getComputedStyle(root).getPropertyValue('--global-pad-x')) || 5;
	const arrowOffsetX = confObj.navArrowOffsetXPercent || 0;  // 기본값 0

	// 전역 패딩 + offset
	leftArrow.style.left = 'calc(' + globalPadX + '% + ' + arrowOffsetX + '%)';
	rightArrow.style.right = 'calc(' + globalPadX + '% + ' + arrowOffsetX + '%)';
}
```

#### 3. JavaScript 수정 - applyMobileBarStyles (line 879-902)

**변경 전:**
```javascript
function applyMobileBarStyles(confObj){
	const isMobile = window.innerWidth <= 768;

	if(!confObj.mobileBarShow || confObj.mode !== 'singleBar' || !isMobile){
		barWrap.style.display = 'none';
	}else{
		barWrap.style.display = 'block';
	}

	// 위치 설정 (항상 실행)
	if(confObj.mobileBarPosition === "bottom"){
		barWrap.style.bottom = '0px';
		// ...
	}
}
```

**변경 후:**
```javascript
function applyMobileBarStyles(confObj){
	const isMobile = window.innerWidth <= 768;

	const shouldShow = confObj.mobileBarShow && confObj.mode === 'singleBar' && isMobile;

	barWrap.style.display = shouldShow ? 'block' : 'none';

	// 표시할 때만 위치 설정
	if(shouldShow){
		if(confObj.mobileBarPosition === "bottom"){
			barWrap.style.bottom = '0px';
			// ...
		}
	}
}
```

#### 4. JavaScript 수정 - getConf (line 817-819)

**변경 전:**
```javascript
navArrowOffsetXPercent: (s.navArrowOffsetXPercent !== undefined && s.navArrowOffsetXPercent !== "") ? +s.navArrowOffsetXPercent : 5,
```

**변경 후:**
```javascript
navArrowOffsetXPercent: (s.navArrowOffsetXPercent !== undefined && s.navArrowOffsetXPercent !== "") ? +s.navArrowOffsetXPercent : 0,
```

---

### 📄 hero-carousel-v3-settings.json

#### 1. navArrowOffsetXPercent 설정 변경 (line 546-551)

**변경 전:**
```json
{
  "id": "style.navArrowOffsetXPercent",
  "label": "좌우 여백 (%)",
  "description": "화면 좌우 끝에서 화살표까지의 여백입니다. 숫자만 입력해주세요. 예: 5",
  "type": "TEXT",
  "placeholder": "5"
}
```

**변경 후:**
```json
{
  "id": "style.navArrowOffsetXPercent",
  "label": "좌우 여백 (%)",
  "description": "전역 패딩 기준에서 추가로 더 떨어뜨릴 여백입니다. 0을 입력하면 전역 패딩만 적용됩니다. 숫자만 입력해주세요. 예: 0",
  "type": "TEXT",
  "placeholder": "0"
}
```

#### 2. property 기본값 변경 (line 926)

**변경 전:**
```json
{
  "navArrowOffsetXPercent": "5"
}
```

**변경 후:**
```json
{
  "navArrowOffsetXPercent": "0"
}
```

---

## 영향 받는 기능

### ✅ 정상 작동
1. **Glow 애니메이션 4방향**
   - 왼쪽 (left-to-right): 왼쪽→오른쪽 스캔
   - 오른쪽 (right-to-left): 오른쪽→왼쪽 스캔
   - 위 (top-to-bottom): 위→아래 스캔
   - 아래 (bottom-to-top): 아래→위 스캔

2. **화살표 버튼 위치**
   - 전역 패딩 5% + 추가 여백 0% = 화면 끝에서 5%
   - 사용자가 전역 패딩을 변경하면 화살표도 함께 이동
   - 추가 여백을 입력하면 전역 패딩에서 더 떨어짐

3. **모바일 게이지바**
   - 조건: mobileBarShow + singleBar + 모바일(≤768px)
   - 모든 조건 충족 시 즉시 표시
   - 위치: 상단 또는 하단 0px 고정

4. **전역 패딩 일관성**
   - 슬라이드 캡션: 전역 패딩 적용 ✓
   - 화살표 버튼: 전역 패딩 적용 ✓
   - 데스크톱 게이지: 전역 패딩 적용 ✓

---

## 테스트 체크리스트

### Glow 애니메이션
- [ ] 왼쪽: 왼쪽에서 오른쪽으로 빛 스캔
- [ ] 오른쪽: 오른쪽에서 왼쪽으로 빛 스캔
- [ ] 위: 위에서 아래로 빛 스캔
- [ ] 아래: 아래에서 위로 빛 스캔
- [ ] 방향 전환 시 즉시 반영됨

### 화살표 버튼
- [ ] 전역 패딩 5%, 추가 여백 0% → 화면 끝에서 5% 위치
- [ ] 전역 패딩 10%, 추가 여백 0% → 화면 끝에서 10% 위치
- [ ] 전역 패딩 5%, 추가 여백 5% → 화면 끝에서 10% 위치
- [ ] 좌우 대칭으로 올바르게 배치됨

### 모바일 게이지바
- [ ] 데스크톱에서 표시 안 됨
- [ ] 모바일(≤768px)에서 "표시" 옵션 활성화 시 즉시 보임
- [ ] 상단 위치 선택 시 화면 최상단에 붙음
- [ ] 하단 위치 선택 시 화면 최하단에 붙음
- [ ] multiStep 모드에서는 표시 안 됨 (정상 동작)

### 전역 패딩 일관성
- [ ] 전역 패딩 변경 시 모든 요소가 함께 이동
  - 슬라이드 캡션
  - 화살표 버튼 (좌우)
  - 데스크톱 게이지 (왼쪽 정렬 시)

---

## 알려진 제한사항

1. **모바일 게이지바는 singleBar 모드에서만 작동**
   - multiStep 모드에서는 의도적으로 숨김
   - 이유: 멀티 스텝은 데스크톱 게이지가 더 적합

2. **전역 패딩은 좌우에만 영향**
   - 상하 패딩 옵션은 v3에서 제거됨
   - 이유: 콘텐츠 수직 정렬을 방해하기 때문

3. **화살표 추가 여백은 %만 지원**
   - px 단위는 지원하지 않음
   - 이유: 반응형 디자인을 위해 %가 더 적합

---

## 업그레이드 가이드

### v3 초기 버전 → v3 수정 버전

#### 설정 변경 없음 (자동 적용)
- Glow 애니메이션 수정 (자동)
- 모바일 게이지바 로직 개선 (자동)

#### 설정 변경 필요
**화살표 여백이 너무 큰 경우:**
- 기존: 화면 끝 기준 5%
- 수정 후: 전역 패딩(5%) + 추가 여백(0%) = 5%
- **변화 없음** (동일한 위치)

**전역 패딩을 사용하지 않는 경우:**
- 전역 패딩을 0%로 설정하면 화살표도 화면 끝에 딱 붙음
- 추가 여백 옵션으로 미세 조정 가능

---

## 파일 크기 변화

| 파일 | 변경 전 | 변경 후 | 차이 |
|------|---------|---------|------|
| hero-carousel-v3-fixed.html | 1,190줄 | 1,193줄 | +3줄 |
| hero-carousel-v3-settings.json | 948줄 | 948줄 | 0줄 |

**총 코드 증가: +3줄 (0.25%)**

---

## 버전 이력

- **v3.1.0** (2025-11-12): 4가지 문제 수정
  - Glow 방향 수정
  - 화살표 전역 패딩 재적용
  - 모바일 게이지바 로직 개선
  - 전역 패딩 일관성 확보

- **v3.0.0** (2025-11-12): 19가지 문제 수정
  - 숫자만 입력 규칙 적용
  - 용어 통일 (PC → 데스크톱)
  - 그림자 프리셋
  - 번호 가로 위치 조절

- **v2.0.0**: 초기 문제 해결 버전
- **v1.0.0**: 최초 개발 버전

---

## 다음 단계

### 추천 테스트 시나리오

1. **Glow 방향 전환 테스트**
   ```
   1. 왼쪽 선택 → hover 시 왼쪽→오른쪽 스캔 확인
   2. 위 선택 → hover 시 위→아래 스캔 확인
   3. 방향 변경 후 즉시 반영되는지 확인
   ```

2. **전역 패딩 일관성 테스트**
   ```
   1. 전역 패딩 5% 설정
   2. 화살표, 게이지, 캡션 위치 확인
   3. 전역 패딩 10%로 변경
   4. 모든 요소가 함께 이동하는지 확인
   ```

3. **모바일 반응형 테스트**
   ```
   1. 데스크톱(>768px): 데스크톱 게이지 표시, 모바일 게이지바 숨김
   2. 모바일(≤768px): 데스크톱 게이지 숨김, 모바일 게이지바 표시
   3. 브라우저 리사이즈 시 즉시 전환되는지 확인
   ```

---

## 문의 및 피드백

추가 문제 발견 시 다음 정보와 함께 보고해주세요:
1. 문제가 발생하는 브라우저 및 버전
2. 화면 크기 (데스크톱/모바일)
3. 설정 값 (특히 전역 패딩, 추가 여백)
4. 스크린샷 또는 동영상
